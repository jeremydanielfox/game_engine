package gae.backend;

import engine.game.Level;

public class LevelManager {

	public LevelManager() {
		// Level level = new Level();
		// level is added, Level object should implement Editable so the Editor
		// class can be used to show any parameters that could be edited.
	}

	public WorldData getWorldData() {
		return null;
	}
}
